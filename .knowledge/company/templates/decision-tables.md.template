# 決策表 — 判斷力的機械化替代（公司模板）

> **部署位置**: 子專案 `.knowledge/decision-tables.md`
> **用途**: 把「需要判斷力的時刻」改寫成查表即可執行的 if-then 規則，讓弱模型 session 不靠感覺做決定。
> **維護規則**: 只收「查表可執行」的規則；散文式教訓放 postmortem-log.md。新增踩坑時由 /pitfall-record 同步。
> **配套**: 搭配 hook-templates/session-start-context.js.template 部署，「核心摘要」段會自動注入每個 session。
> **填寫說明**: {{...}} 為佔位符，依專案填入。表 1/2/3/5 的骨架跨專案通用，直接沿用。

---

## 核心摘要（每 session 自動注入）

<!-- INJECT:START -->
1. **完成 = 證據**：宣告任何任務完成前，貼出實際執行過的驗證指令＋輸出。貼不出 = 沒有完成。「未驗證」是合法狀態，寫出來；把推論當事實是違規。
2. **除錯熔斷**：同一症狀第 3 次出現 → 停止修單點，改為端到端驗整條 pipeline 每一段。
3. **資料/紀錄類修復**：必須 query 實際資料源證實資料真的變了。單元測試綠不算數。
4. **不確定就去讀真相來源，不要猜**；連「現在幾點」都用 `node -e "console.log(new Date().toISOString())"` 取得。
5. **被 hook/CI 擋下**：基準線是綠的，紅 = 你弄壞了 → 修到綠。確認 false positive → /pitfall-record + backlog，禁止靜默繞過。
6. **越權熔斷**：要刪除/覆蓋既有功能或資料、偏離計畫範圍、規範文件互相矛盾 → 停下來問。
{{視專案補充，注入段總長 ≤ 15 行}}
<!-- INJECT:END -->

---

## 1. 完成證據等級表

| 變更類型 | 最低證據（實際執行過的指令＋輸出摘要） |
|---------|------------------------------------|
| 純文件 | `git status` 顯示只動了文件 |
| 程式碼 | lint ＋ typecheck/編譯 ＋ 相關單元測試輸出 |
| 需 build/部署才生效的變更 | 上述 ＋ build 產物時間戳晚於原始碼的證明 |
| 資料/紀錄類修復 | 實際資料源 query 前後對比。單測綠不算數 |
| CI / hook / 自動化本身 | 判定測試 ＋ 煙霧測試全綠 |
| {{專案特有類型}} | {{對應證據}} |

**鐵律**：沒有貼得出來的證據 = 沒有完成。

## 2. 除錯熔斷表

| 同一症狀第幾次 | 允許的行為 |
|--------------|-----------|
| 第 1 次 | 修最直接的失敗點，附證據 |
| 第 2 次 | 先列 pipeline 全部環節、逐段標「已驗證/未驗證」，只修驗過確實壞的那段 |
| 第 3 次起 | 停。端到端驗整條 pipeline ＋ query 權威資料源證實症狀 ＋ 評估換一條更穩的鏈路 |

**判定假修復三問**：(1) 用戶實際跑的版本真的含這個 fix 嗎？(2) 看到的「綠」測到修復本體還是 mock？(3) 症狀的權威資料源現在長什麼樣？

## 3. 升級決策表

| 情境 | 動作 |
|------|------|
| 實作細節且規範有覆蓋 | 自行決定，不用問 |
| 需求含糊但有合理預設 | 採預設 ＋ 記下「假設：…」 ＋ 繼續 |
| 規範文件互相矛盾 | 停，引用兩處原文問上級 |
| 發現規範文件本身錯了 | 先提案改文件，不得跳過文件直接改 code |
| 要刪除/覆蓋既有功能或資料 | 停下來問 |
| 要偏離計畫範圍 | 停下來問 |

## 4. 不確定性協議（真相來源表）

| 你想知道的 | 真相來源（讀，不要猜） |
|-----------|---------------------|
| 現在幾點 | `node -e "console.log(new Date().toISOString())"` |
| 過去的教訓 | postmortem-log.md 快速參考表 |
| {{API 格式 / DB 欄位 / 設定生效與否…}} | {{專案對應的原始碼位置或實測方法}} |

## 5. 警報回應表

| 情況 | 正確反應 | 禁止 |
|------|---------|------|
| 你剛改的東西讓檢查變紅 | 修到綠再結束 | 視為預存問題 |
| 有證據是 false positive | /pitfall-record ＋ backlog 修 hook；換不觸發的等效指令 | 靜默繞過不記錄 |
| 檢查永遠紅/永遠綠 | 失效警報，最高優先修 | 矇混過去 |

## 6. Session 開場自檢

1. `pwd` — 確認在正確的專案目錄
2. `git branch --show-current && git status -sb` — 確認分支與 working tree 狀態
3. 接到任務先查表 1，開工前就知道完成時要交什麼證據

## 7. 制度修改守則

| 想做的事 | 前置條件 |
|---------|---------|
| 新增會 block 的 hook | 目前基準線能過 ＋ 邏輯可 export ＋ false-positive 測試 ＋ 煙霧測試案例 |
| 加規則進 CLAUDE.md | 先問能不能用 hook 強制；索引原則不可破 |
| 加內容進本文件 | 必須是查表可執行的 if-then；注入段 ≤ 15 行 |
